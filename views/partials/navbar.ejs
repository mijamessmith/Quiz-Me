<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#" id="landing">Think You Know Me</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/home">Profile</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#" id="logoutBtn">Logout</a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<script>
  const landing = document.getElementById("landing");
  const logout = document.getElementById("logoutBtn");
  
  landing.addEventListener("click", function (event) {
    event.preventDefault()
    console.log("Landing Btn has been clicked")
 
    let ajaxReq; //sometimes declaring and initializing has issues
    ajaxReq = $.ajax({
    url: '/', //name of the route. If we want more params we use & to separate.
  type: 'GET',
  datatype: "json",
  data: "" //we're not sending
  });

  ajaxReq.done(function (data) {
  //change car color in front end with the data
  if (data !== undefined) {
  window.location.replace(`http://localhost:3000/`); //sending a new addresss to load
  }
  });
  });

  logout.addEventListener("click", function (event) {
  event.preventDefault()
  console.log("Logout Btn has been clicked")

  let ajaxReq; //sometimes declaring and initializing has issues
  ajaxReq = $.ajax({
  url: '/logout', //name of the route. If we want more params we use & to separate.
  type: 'GET',
  datatype: "json",
  data: "" //we're not sending
  });

  ajaxReq.done(function (data) {
  //change car color in front end with the data
  if (data !== undefined) {
  window.location.replace(`http://localhost:3000/logout`); //sending a new addresss to load
  }
  });
  });

</script>